An API usually means:

1. client sends request
2. backend sends data
3. data is often in JSON

Sending data is easy. Receiving data with plain Node is more manual.

When client sends data using POST, Node receives it in chunks.